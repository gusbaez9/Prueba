let numeros= [1,2,3,4,5];
let sum=0;
for(let i=0; i<numeros.length; i++) {
    sum+=numeros[i];
}

/*
let numeros= [1,2,3,4,5]; //establezco una variable (array) que contenga algunos valores.
let sum=0; //establezco una variable que comience con un valor 0.
for(let i=0; //establezco una variable "i" que comience en valor 0.
i<numeros.length; //la condición es que "i" se repita las mismas veces que la cantidad de caracteres de la variable "numeros".
i++) //establezco que la variable "i" va a incrementarse.
{
    sum+=numeros[i]; //la variable "sum" va a sumar cada caracter de "numeros" y devuelve el total.
}*/